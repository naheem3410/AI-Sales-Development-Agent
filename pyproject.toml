[project]
name = "sda-platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.118.3",
    "uvicorn>=0.37.0",
    "dotenv>=0.9.9",
    "openai-agents[litellm]>=0.0.15",
    "pydantic>=2.11.10",
]
[tool.uv]
package = false
